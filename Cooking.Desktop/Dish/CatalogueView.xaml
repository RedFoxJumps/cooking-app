<UserControl x:Class="Cooking.Desktop.DishArea.CatalogueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cooking.Desktop.DishArea"
             xmlns:init="clr-namespace:Cooking.Desktop.Shared"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance local:ICatalogueViewModel}">
    <i:Interaction.Behaviors>
        <init:InitializeBehavior />
    </i:Interaction.Behaviors>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.8*" />
            <RowDefinition Height="0.2*" />
        </Grid.RowDefinitions>

        <ListView Grid.Row="0"
                  ItemsSource="{Binding Dishes}"
                  SelectedItem="{Binding SelectedDish}"
                  SelectionMode="Single" />

        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                Command="{Binding ShowNewDishViewCommand}"
                Content="{DynamicResource NewDish}" />

            <Button Grid.Column="1"
                Command="{Binding ShowDishEditViewCommand}"
                Content="{DynamicResource Edit}" />
        </Grid>
    </Grid>
</UserControl>

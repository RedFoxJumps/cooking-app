<UserControl x:Class="Cooking.Desktop.DishArea.DishView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cooking.Desktop.DishArea"

             xmlns:models="clr-namespace:Cooking.Contracts.Models;assembly=Cooking.Contracts"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance models:Dish}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Name}"
                   HorizontalAlignment="Left" VerticalAlignment="Center" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <ItemsControl Grid.Column="0">
                
            </ItemsControl>
            
            <Button
        </Grid>
    </Grid>
</UserControl>

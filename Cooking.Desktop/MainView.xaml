<Window x:Class="Cooking.Desktop.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cooking.Desktop"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800"
        d:DataContext="{d:DesignInstance local:IMainViewModel}">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <ContentControl Grid.Column="0" Content="{Binding MenuHistoryViewModel}" />
        <ContentControl Grid.Column="1" Content="{Binding CatalogueViewModel}" />
        <ContentControl Grid.Column="2" Content="{Binding NewMenuViewModel}" />
    </Grid>
</Window>

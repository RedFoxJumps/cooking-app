<UserControl x:Class="Cooking.Desktop.MenuArea.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cooking.Desktop.MenuArea"
             xmlns:models="clr-namespace:Cooking.Services.Models;assembly=Cooking.Services"

             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance models:Menu}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0">
                <Run Text="{DynamicResource CreatedAt}" /><Run Text=":" />
                <Run Text="{Binding CreatedAt}" />
            </TextBlock>
            
            <TextBlock Grid.Column="1" Text="{Binding Note}" />
        </Grid>
        
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Dishes}" />
    </Grid>
</UserControl>
